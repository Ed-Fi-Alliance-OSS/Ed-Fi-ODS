{
  "WebApiVersionUrl": "${VERSION_URL}",
  "EnableOneRoster": "${ENABLE_ONEROSTER}",
  "OneRosterMetadataUrl": "${ONEROSTER_METADATA_URL}",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "SwaggerUIOptions": {
    "OAuthConfigObject": {
      "ClientSecret": "${SAMPLE_SECRET}",
      "ClientId": "${SAMPLE_KEY}"
    }
  }
}
